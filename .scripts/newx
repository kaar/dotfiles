#!/bin/sh
[ ! -n "${SCRIPTS}" ] && echo "SCRIPTS not set" && exit 1;
[ ! -n "${1}" ] && echo "usage: newscript <name>" && exit 1;

path="${SCRIPTS}/${1}"

if [ -e "${path}" ]; then
  exec vim "${path}"
  exit 0
fi

printf "#!/bin/sh\n\n" > "${path}"
chmod +x "${path}"
exec vim +2 "${path}"
